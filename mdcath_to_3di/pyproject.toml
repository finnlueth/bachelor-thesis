[build-system]
requires = ["setuptools", "setuptools-scm"]
build-backend = "setuptools.build_meta"

[project]
name ="src"
version = "0.0.1"
dependencies = []

[tool.setuptools.packages.find]
include = ["src*"]